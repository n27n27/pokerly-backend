# Pokerly Backend

- Spring Boot 3.x / Java 17 / Gradle
- DB: MariaDB (Docker compose)
- JPA: ddl-auto=validate (스키마는 SQL/Flyway로만 관리)

## Dev
1) Docker Desktop 실행
2) `docker compose up -d`
3) `./gradlew bootRun`
